import 'package:flutter/material.dart';

class DataPenyewa extends StatelessWidget {
  const DataPenyewa({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('DataPenyewa'),
      ),
    );
  }
}
